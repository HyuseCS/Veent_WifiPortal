node_modules

#IDEs
.vscode
.idea

# Output (match in every workspace package, not just repo root)
.output
.vercel
.netlify
.wrangler
.svelte-kit
build
dist

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.prod.example
!.env.test

# Generated production service config (contains secrets) — see scripts/setup-prod.ts
# Anchored to repo root so it does NOT swallow docs/deploy/ (the deployment guide).
/deploy/

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results
.playwright-mcp/

# graphify (knowledge-graph build output)
graphify-out/

# Python venv (graphify tooling)
.venv/

start_db.sh

# AI agent tooling — machine-local, not part of the app (vendored skills, configs)
.agents/
.claude/
.codex/
.gemini/
.vibecode-backup
.vc-version
.vc-installed-files
CLAUDE.md
GEMINI.md
AGENTS.md
.vibecode-backup*
