/**
 * Layout chrome — the persistent shell around authenticated pages.
 * Import as: `import { Sidebar, Topbar } from '$lib/components/layout';`
 */
export { default as Sidebar } from './Sidebar.svelte';
export { default as Topbar } from './Topbar.svelte';
export { default as ModeToggle } from './ModeToggle.svelte';
export { default as LayoutSwitcher } from './LayoutSwitcher.svelte';
