/**
 * Convenience re-export of every component group.
 * Prefer the specific barrel (`$lib/components/ui` etc.) in app code; this exists
 * for the rare spot that pulls from multiple groups.
 */
export * from './ui';
export * from './layout';
export * from './feature';
