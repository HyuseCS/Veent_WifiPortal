# Shared database connection used by drizzle-kit (migrations / studio).
# Same value as each app's DATABASE_URL — all modules share ONE database.
DATABASE_URL="postgres://root:mysecretpassword@localhost:5432/local"
