# Deploy runbook — moved

The deploy guide now lives at **[`docs/deploy/README.md`](../deploy/README.md)**. The single-VM Docker
compose path is **Part 1 — Production (Docker)** there; the shared router/Sentry/secrets steps are in
the `docs/deploy/` references.
